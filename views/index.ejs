<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>todo-list</title>
    <link rel="stylesheet" href="css/styles.css">
</head>
<body>
    <h1 id="myheader"> Today </h1>
    <div class="box">
          
       <% for(var i=0;i<tasks.length;i++){ %>
        <form action="/delete" method="post" >
            <div class="item">
                <input type="checkbox" name="checkbox" value="<%=tasks[i]._id%>" onchange="this.form.submit()">
                <p><%= tasks[i].task %></p>
                
            </div>
             
        </form>
   <% } %>

<form class="item" action="/" method="post" autocomplete="off">
    <input type="text" name="task" placeholder="input your task here">
    <button type="submit">+</button>
</form>
    </div>
    
</body>
</html>